{
  "cli": {
    "version": ">= 12.5.1",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal"
    },
    "production": {
      "android": {
        "buildType": "apk", // Specify APK build type
        "autoIncrement": true
      },
      "distribution": "store" // Change to "store" or "internal" as needed
    }
  },
  "submit": {
    "production": {}
  }
}
